  const videos = require("./videos");
  const users = require('./users');
  const jwtauth = require('./jwtauth');

  module.exports = {
      videos: videos,
      users: users,
      jwtauth: jwtauth
  };
